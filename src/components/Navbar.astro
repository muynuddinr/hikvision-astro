<script>
  // Initialize AOS
  import AOS from 'aos';
  import 'aos/dist/aos.css';

  AOS.init({
    duration: 800,
    once: true,
  });

  // Mobile menu toggle
  document.addEventListener('DOMContentLoaded', () => {
    const menuBtn = document.getElementById('menu-toggle');
    const sidebar = document.querySelector('nav');
    
    menuBtn?.addEventListener('click', () => {
      sidebar?.classList.toggle('translate-x-0');
      menuBtn?.classList.toggle('open');
    });

    // Close menu when clicking outside
    document.addEventListener('click', (e) => {
      if (!sidebar?.contains(e.target as Node) && 
          !menuBtn?.contains(e.target as Node) && 
          sidebar?.classList.contains('translate-x-0')) {
        sidebar?.classList.remove('translate-x-0');
        menuBtn?.classList.remove('open');
      }
    });
  });
</script>

<!-- Hamburger Button -->
<button 
  id="menu-toggle" 
  class="fixed top-4 left-4 z-50 p-3 rounded-lg hover:bg-gray-100 transition-colors duration-300"
>
  <div class="w-6 h-5 relative transform transition-all duration-300">
    <span class="absolute h-0.5 w-full bg-red-600 transform transition-all duration-300 rotate-0 top-0"></span>
    <span class="absolute h-0.5 w-full bg-red-600 transform transition-all duration-300 opacity-100 top-2"></span>
    <span class="absolute h-0.5 w-full bg-red-600 transform transition-all duration-300 rotate-0 top-4"></span>
  </div>
</button>

<nav class="fixed h-full w-64 z-40 left-0 top-0 transform transition-transform duration-300 -translate-x-full">
  <!-- Enhanced Background Elements -->
  <div class="absolute inset-0 bg-gradient-to-b from-white via-red-50/30 to-white overflow-hidden">
    <div class="absolute top-0 left-0 w-full h-full bg-[radial-gradient(circle_at_30%_30%,rgba(239,68,68,0.15),transparent_70%)] animate-pulse-slow"></div>
    <div class="absolute bottom-0 right-0 w-full h-full bg-[radial-gradient(circle_at_70%_70%,rgba(220,38,38,0.12),transparent_70%)] animate-pulse-slower"></div>
    
    <!-- Floating shapes -->
    <div class="absolute">
      {[...Array(6)].map((_, i) => (
        <div class={`floating-shape-${i + 1} absolute rounded-full mix-blend-multiply filter blur-xl opacity-70`}></div>
      ))}
    </div>
  </div>

  <div class="relative h-full px-6 backdrop-blur-sm">
    <!-- Rest of your nav content -->
    <div class="flex-shrink-0 pt-16 pb-10">
      <a href="/" class="text-2xl font-semibold text-red-600">
        Your Logo
      </a>
    </div>

    <!-- Menu Items -->
    <div class="flex flex-col space-y-6">
      <a 
        href="/" 
        class="flex items-center space-x-3 text-gray-600 hover:text-blue-600 transition-colors duration-300"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
        </svg>
        <span>Home</span>
      </a>

      <a 
        href="/About" 
        class="flex items-center space-x-3 text-gray-600 hover:text-blue-600 transition-colors duration-300"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
        </svg>
        <span>About</span>
      </a>

      <a 
        href="/Service" 
        class="flex items-center space-x-3 text-gray-600 hover:text-blue-600 transition-colors duration-300"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
        <span>Services</span>
      </a>

      <a 
        href="/Contact" 
        class="flex items-center space-x-3 text-gray-600 hover:text-blue-600 transition-colors duration-300"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
        <span>Contact</span>
      </a>
    </div>
  </div>
</nav>

<style>
  /* Added animation styles */
  .animate-pulse-slow {
    animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .animate-pulse-slower {
    animation: pulse 5s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .floating-shape-1 { background: rgba(239,68,68,0.1); width: 300px; height: 300px; top: 10%; left: 15%; animation: float 8s infinite; }
  .floating-shape-2 { background: rgba(220,38,38,0.1); width: 200px; height: 200px; top: 60%; right: 10%; animation: float 9s infinite; }
  .floating-shape-3 { background: rgba(239,68,68,0.05); width: 150px; height: 150px; top: 30%; right: 20%; animation: float 7s infinite; }
  .floating-shape-4 { background: rgba(239,68,68,0.07); width: 250px; height: 250px; bottom: 20%; left: 5%; animation: float 10s infinite; }
  .floating-shape-5 { background: rgba(220,38,38,0.08); width: 180px; height: 180px; top: 40%; left: 30%; animation: float 11s infinite; }
  .floating-shape-6 { background: rgba(239,68,68,0.06); width: 220px; height: 220px; bottom: 30%; right: 15%; animation: float 9s infinite; }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  nav {
    overflow-y: auto;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  nav::-webkit-scrollbar {
    display: none;
  }

  /* Hamburger Animation */
  #menu-toggle.open span:first-child {
    top: 10px;
    transform: rotate(45deg);
  }
  
  #menu-toggle.open span:nth-child(2) {
    opacity: 0;
  }
  
  #menu-toggle.open span:last-child {
    top: 10px;
    transform: rotate(-45deg);
  }
</style>
